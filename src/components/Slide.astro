---
interface Props {
    img: string;
    title: string;
    url: string;
}

const props = Astro.props
---

<div class="slide">
    <a href={props.url} target="_blank">
        <img src={props.img} alt={props.title} />
        <h3>{props.title}</h3>
    </a>
</div>